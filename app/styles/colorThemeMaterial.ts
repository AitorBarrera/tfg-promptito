import { createTheme } from "@mui/material/styles";

export const colorThemeMaterial = createTheme({
  palette: {
    primary: {
      main: "#02b59f",
    },
    secondary: {
      main: "#ff9f03",
    },
  },
});
