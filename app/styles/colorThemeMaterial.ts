import { createTheme } from "@mui/material/styles";

export const colorThemeMaterial = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#02b59f",
    },
    secondary: {
      main: "#ff9f03",
    },
  },
});
